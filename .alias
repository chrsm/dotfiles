alias vim="nvim"
alias vi="nvim"
alias tmux="tmux -2"

alias "git-patch"="git add -p"

alias cdsrc="cd /data/src"

alias dco="docker-compose"

alias "cd.."="cd .. ; echo You are an idiot, it\'s cd .."
alias genpwd="cat /dev/urandom|strings|head -n 5| tr -d '\n\"\`\\t'|head -c 30|xsel -ib && xsel -o"
alias ssh="assh wrapper ssh"
alias shellcheck="docker run -it -v \`pwd\`:/opt/host -w /opt/host jess/shellcheck shellcheck"
alias clang++="clang++ -MJ compile_commands.json -fsanitize=address -Wall -Wextra -Wfloat-equal -std=c++17"
alias g++="g++ -Wall -Wextra -Wfloat-equal -std=c++17"

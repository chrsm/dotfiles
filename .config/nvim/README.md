Requires 0.5+ for the LSP builtins.

### On OSX or whatever apple calls it...

```
brew install ripgrep
brew install luarocks
brew install cmake
brew install --HEAD luajit
brew install --HEAD neovim
```


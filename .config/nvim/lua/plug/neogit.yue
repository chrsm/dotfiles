import "macros" as { $ }

export default {
  setup: (...) ->
    ng = require "neogit"
    
    ng.setup {
      kind: "split"
      integrations:
        diffview: true
    }

    $nosilent '<leader>g', '<cmd>Neogit<CR>'
}

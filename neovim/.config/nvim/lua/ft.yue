import "macros" as { $ }

types =
  * "yue"
  * "go"
  * "cpp"
  * "lua"
  * "proto"
  * "php"
  * "dsa"
  * "crystal"
  * "terraform"

export default {
  setup: ->
    require "ft.#{ft}" for _, ft in ipairs types
}

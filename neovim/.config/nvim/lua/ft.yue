import 'macros' as { $ }

types =
  * 'yue'
  * 'go'
  * 'cpp'
  * 'lua'
  * 'proto'
  * 'php'
  * 'dsa'
  * 'crystal'
  * 'terraform'
  * 'typescript'

export default {
  setup: ->
    require "ft.#{ft}" for ft in *types -- yuecheck:ignore
}

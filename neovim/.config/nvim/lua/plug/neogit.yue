import "macros" as { $ }

cfg =
  kind: "split"
  integrations:
    diffview: true

export default {
  config: ->
    ng = require "neogit"
    
    ng.setup cfg

    $nosilent '<leader>g', '<cmd>Neogit<CR>'
}

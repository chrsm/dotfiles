import "macros" as { $ }

cfg =
  kind: "split"
  integrations:
    diffview: true

export default {
  setup: (...) ->
    ng = require "neogit"
    
    ng.setup cfg

    $nosilent '<leader>g', '<cmd>Neogit<CR>'
}
